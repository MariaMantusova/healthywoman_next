/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images%20.unsplash.com", "images.unsplash.com", "images.unsplash%20.com"],
    },
}

module.exports = nextConfig
