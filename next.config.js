/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "www.edamam.com", "edamam-product-images.s3.amazonaws.com"],
    },
}

module.exports = nextConfig
